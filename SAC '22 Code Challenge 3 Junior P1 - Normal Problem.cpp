#include <bits/stdc++.h>
using namespace std;

int main()
{
    cin.tie(0); cout.tie(0); cin.sync_with_stdio(0);
    string str1;
    cin >> str1;
    if (str1.find("demello") != -1) cout << "liar" << endl;
    else cout << "what are we going to do?" << endl;
}
